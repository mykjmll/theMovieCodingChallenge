// import { selectFavoritesPageDomain } from '../selectors';

describe('selectFavoritesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
